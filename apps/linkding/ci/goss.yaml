## linkding serves via uwsgi (http) on 9090 with an embedded SQLite DB,
## so it boots and listens without any external state. The listening
## port is the reliable health signal.
port:
  tcp:9090:
    listening: true
