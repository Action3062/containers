## Cleanuparr is a .NET service that serves its embedded web UI on 11011
## and boots without external dependencies (config is created on first
## run). The listening port is the reliable health signal.
port:
  tcp:11011:
    listening: true
